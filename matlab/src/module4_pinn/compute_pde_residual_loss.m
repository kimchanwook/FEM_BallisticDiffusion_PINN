function loss = compute_pde_residual_loss(varargin)
%COMPUTE_PDE_RESIDUAL_LOSS Placeholder for automatic-differentiation PDE residuals.
% MATLAB implementation depends on the chosen dlnetwork training loop.
% This function intentionally errors until the network training stage is active.
error(['compute_pde_residual_loss is a scaffold. Implement after selecting ', ...
       'the exact state vector, time sampling, and AD training loop.']);
end
